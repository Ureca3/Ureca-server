<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ureca.unity.domain.user.mapper.UserMapper">

    <select id="findByProviderAndProviderId"
            resultType="com.ureca.unity.domain.user.model.User">
        SELECT
            user_id AS id,
            provider,
            provider_id AS providerId,
            email,
            name,
            role,
            terms_agreed_at AS termsAgreedAt
        FROM users
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
          AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="user_id">
        INSERT INTO users (
            provider,
            provider_id,
            email,
            name,
            role
        ) VALUES (
                     #{provider},
                     #{providerId},
                     #{email},
                     #{name},
                     #{role}
                 )
    </insert>

    <select id="findById" resultType="com.ureca.unity.domain.user.model.User">
        SELECT
            user_id AS id,
            provider,
            provider_id AS providerId,
            email,
            name,
            role,
            terms_agreed_at AS termsAgreedAt
        FROM users
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
    </select>

    <update id="softDeleteById">
        UPDATE users
        SET deleted_at = NOW()
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
    </update>

    <select id="findAnyByProviderAndProviderId"
            resultType="com.ureca.unity.domain.user.model.User">
        SELECT
            user_id AS id,
            provider,
            provider_id AS providerId,
            email,
            name,
            role,
            terms_agreed_at AS termsAgreedAt,
            deleted_at AS deletedAt
        FROM users
        WHERE provider = #{provider}
          AND provider_id = #{providerId}
    </select>

    <update id="restoreById">
        UPDATE users
        SET
            deleted_at = NULL,
            email = #{email},
            name = #{name},
            terms_agreed_at = NULL
        WHERE user_id = #{userId}
    </update>

    <update id="updateTermsAgreedAt">
        UPDATE users
        SET terms_agreed_at = #{termsAgreedAt}
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
    </update>


</mapper>
